<script setup lang="ts">import { useRoute } from 'vue-router'; import PageHeader from '../components/PageHeader.vue'; import EmptyState from '../components/EmptyState.vue'; const route=useRoute(); const exclusion=route.name==='exclusion-rules';</script>
<template><PageHeader :title="exclusion ? 'Reglas de exclusión' : 'Reglas de ISR'" :description="exclusion ? 'Administra criterios auditables para retirar registros del total.' : 'Consulta la clasificación priorizada de conceptos de ISR.'" /><EmptyState :title="exclusion ? 'No hay reglas de exclusión confirmadas' : 'Reglas iniciales instaladas'" :description="exclusion ? 'Las reglas deben basarse en valores confirmados por la institución; no se asumirá que un código significa retenido o cancelado.' : 'Las reglas seed viven en SQLite y se aplican por prioridad. La edición visual se habilitará después de validar el catálogo institucional.'" icon="bi-funnel" /></template>
